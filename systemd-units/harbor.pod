[Unit]
Description=Harbor Pod
#Requires=harbor.network
#After=harbor.network
Requires=harbor-network.service
After=harbor-network.service

#[Pod]
#Network=harbor.network

[Install]
WantedBy=default.target
